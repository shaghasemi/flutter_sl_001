import 'package:flutter/material.dart';

class ProductFullScreenWidget extends StatelessWidget {
  const ProductFullScreenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
