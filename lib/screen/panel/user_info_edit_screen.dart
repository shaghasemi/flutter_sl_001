import 'package:flutter/material.dart';

class UserInfoEditScreen extends StatelessWidget {
  const UserInfoEditScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
