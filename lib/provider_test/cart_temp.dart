import 'package:flutter/material.dart';

class CartTemp extends StatelessWidget {
  const CartTemp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
